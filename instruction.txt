uvicorn api:app --reload --port 8100

Open the frontend

    Just double-click index.html in your project_root (or right-click → “Open with Chrome/Edge”).

    The JavaScript in the page will call the API at http://localhost:8100/predict_multi.